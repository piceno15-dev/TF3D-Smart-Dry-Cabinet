# Guía de contribución
Usar ramas `cad/*`, `electronics/*`, `firmware/*`, `backend/*` y `frontend/*`.

Formato de commit recomendado:
```text
feat(cad): add initial cabinet assembly
fix(cad): correct shelf depth
docs: update manufacturing notes
```
Todo cambio dimensional debe actualizar configuración, lista de corte y changelog.
